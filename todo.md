
- to fix :
	- add methods to `Input` to check the type of key pressed (e.g. `is_key_escape`, `is_key_left`, `is_key_char`, `get_key_char`, etc.) and use these instead of getting the `input_key` and manually checking  

- to improve : 
	- `add` : remove this since it is done using the windows file dialog

- to add :
	- commands left to add to new ui : save, load, set wallpaper

	