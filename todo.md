
- to fix :
	- create some class that holds all the commands that relate the app and don't anywhere to go like
		- add wallpaper
		- add folder
	- fix removing folder should not show the default folder

- to improve : 
	- change `add_folder` in `Gloabal.cpp` to work correctly with the new structure
	- use `add_wallpaper` in `action` function
	- use `add_folder` in `action` function and `AddMenu` class

- to add :
	- commands left to add to new ui : load

	